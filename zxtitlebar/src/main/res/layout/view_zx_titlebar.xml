<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/llRoot"
    android:layout_width="match_parent"
    android:layout_height="45dp"
    android:background="@color/white">

    <FrameLayout
        android:id="@+id/flLeft"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/tvLeft"
            style="@style/titleBar_text_left"
            android:visibility="invisible"
            tools:text="leftText" />

        <ImageView
            android:id="@+id/ivLeft"
            style="@style/titleBar_image_left"
            android:visibility="invisible"
            tools:visibility="visible" />
    </FrameLayout>

    <TextView
        android:id="@+id/tvTitle"
        style="@style/titleBar_title_text"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:ellipsize="end"
        android:singleLine="true"
        tools:text="title" />


    <FrameLayout
        android:id="@+id/flRight"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/tvRight"
            style="@style/titleBar_text_right"
            android:visibility="invisible"
            tools:text="rightText"
            tools:visibility="visible" />

        <ImageView
            android:id="@+id/ivRight"
            style="@style/titleBar_image_right"
            android:visibility="invisible" />
    </FrameLayout>

</LinearLayout>